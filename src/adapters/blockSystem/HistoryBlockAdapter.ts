import { DetailSection, VersionBean } from "../../persistance/data/VersionBean";

export class HistoryBlockAdapter {

    async saveVersionBlock(bean: DetailSection, versionBean: VersionBean): Promise<void> {

    }

}